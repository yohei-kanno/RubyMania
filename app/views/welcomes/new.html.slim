.flex
  .top-right
    .user-ranking-header
      / = t("dict.service")
      .swiper-container
        .swiper-wrapper
          - @categories.each do |category|
            .swiper-slide
              span.box-title = category.name 
              .title
                = link_to "Let's start!!", questions_path(name: category.name), class: "container-disabled-title", data: {confirm: "#{category.name}編を開始しますか？"}
              .sub-title
                = "こんな問題が出題されます！"
                - unless category.questions.count == 0
                  - q = category.questions.first
                  .box-container
                    = q.title
                  .box-content
                    = q.content
                  .box-choices
                    - q.choices.each do |choice|
                      .box-choice_content
                        o= choice.content

              
        .swiper-pagination.swiper-pagination-black
        .swiper-button-prev.swiper-custom-button
          i.fa.fa-angle-left
        .swiper-button-next.swiper-custom-button
          i.fa.fa-angle-right
            
  .top-left.text-left
    .user-ranking-header
      = "ランキング"
      - user_rank.each.with_index(1) do |(j ,user),i|
        .wrapper
          .wrap
            - if user
              = image_tag j, class: "image-font"
              span.ml-5 = "#{i}位"
              span.ml-10 = "Lv#{user.level}"
              span.ml-10 = user.nickname
            - else
              = "まだユーザーがいません"
          
script src="https://unpkg.com/swiper/swiper-bundle.js" 
script src="https://unpkg.com/swiper/swiper-bundle.min.js" 
= javascript_pack_tag "swiper/swiper.js"