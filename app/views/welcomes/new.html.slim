#particles-js
= render "top_wrapper"
= render "swiper_chart"
= render "tab_menu_and_content", categories: @categories
= render "javascript"

- Category.count.times  do |i|
  div id="user_average_#{i}" data-json="#{current_user.average_score(i).to_json}" 
script src="https://cdn.jsdelivr.net/particles.js/2.0.0/particles.min.js" 
javascript:
  particlesJS.load('particles-js', '/assets/particles.json');  