= link_to t("variable.retry", name: @category.name),  questions_path(name: @category.name), class: "btn btn-primary mb-3", style: "width: 80%;"
= link_to t("link.to_toppage"), root_path, class: "btn btn-primary mb-3", style: "width: 80%;"

.title.mb-3 
  = "[回答]#{@category.name}編"
div
  = t("variable.your-score", point: "#{@point}")
  
= render "shared/answer_form",  {questions: @questions}
                
= link_to t("variable.retry", name: "#{@category.name}"),  questions_path(name: @category.name), class: "btn btn-primary mb-3", style: "width: 80%;"
= link_to t("link.to_toppage"), root_path, class: "btn btn-primary mb-3", style: "width: 80%;"
